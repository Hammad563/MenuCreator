<nav class="bg-white shadow" data-controller="dashboard">
    <div class="max-w-7xl mx-auto px-4 sm-px-6 lg-px-8">
        <div class="flex justify-between h-16">
            <div class="flex">
                <div class="flex-shrink-0 flex items-center">
                    <%= image_tag "menu_logo.jpg", class: "block lg:hidden h-14 w-auto" %>
                    <%= image_tag "menu_logo.jpg", class: "hidden lg:block h-14 w-auto" %>
                </div>
                <div class="hidden sm:items-center sm:flex sm:ml-6 sm:flex sm:space-x-8">
                    <%= link_to "Dashboard", dashboard_path, class: "#{dashboard_header_class("dashboard")}" %>
                    <%= link_to "Settings", settings_path, class: "#{dashboard_header_class("settings")}"%>
                </div>
            </div>
            <div class="hidden sm:ml-6 sm:flex sm:items-center">
                <%= link_to "Log out", destroy_user_session_path, data: { "turbo-method": :delete }, class: "h-full border-transparent text-gray-500 hover:border-blue-300 hover:text-blue-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
            </div>
            <div class="-mr-2 flex items-center sm:hidden">
                <!-- Mobile -->
                <button
                data-dashboard-target="menuButton"
                type="button"
                class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-gray-500 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-blue-500" aria-controls="mobile-menu" aria-expanded="false"
                >
                <span class="sr-only">Open main menu</span>
                <svg
                    data-dashboard-target="openIcon"
                    class="block h-6 w-6"
                    xmlns="http://www.w3.org/2000/svg"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke-width="2"
                    stroke="currentColor"
                    aria-hidden="true"
                >
                    <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
                </svg>
                <svg
                    data-dashboard-target="closeIcon"
                    class="hidden h-6 w-6"
                    xmlns="http://www.w3.org/2000/svg"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke-width="2"
                    stroke="currentColor"
                    aria-hidden="true"
                >
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                </svg>
                </button>
            </div>
        </div>
    </div>
    <div class="hidden" data-dashboard-target="mobileMenu">
        <div class="pt-2 pb-3 space-y-1">
        <!-- Current: "bg-green-50 border-green-500 text-green-700", Default: "border-transparent text-gray-500 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-700" -->
            <%= link_to "Dashboard", dashboard_path, class: "#{dashboard_header_class("dashboard")}" %>
            <%= link_to "Settings", settings_path, class: "#{dashboard_header_class("dashboard")}" %>
            <%= link_to "Log out", destroy_user_session_path, data: { "turbo-method": :delete }, class: "border-transparent text-gray-500 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-700 block pl-3 pr-4 py-2 border-l-4 text-base font-medium" %>
        </div>
  </div>
</nav>